{
  "name": "redisng",
  "version": "1.0.1",
  "description": "A lightweight but fully functional Promise based Redis Client for NodeJS",
  "main": "lib/index.js",
  "dependencies": {
    "redis-proto": "^2.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-steelbrain": "^4.0.1",
    "eslint-config-steelbrain": "^1.0.4",
    "flow-bin": "^0.32.0",
    "jasmine-fix": "^1.0.1",
    "sb-stream-promise": "^1.0.0"
  },
  "scripts": {
    "lint": "(flow check) && (eslint .)",
    "test": "apm test",
    "clean": "rm -rf lib",
    "compile": "npm run clean && babel src --out-dir lib",
    "watch": "npm run clean && babel src --out-dir lib --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/steelbrain/RedisNG-Node.git"
  },
  "keywords": [
    "redis",
    "promise",
    "es6"
  ],
  "author": "steelbrain",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/steelbrain/RedisNG-Node/issues"
  },
  "homepage": "https://github.com/steelbrain/RedisNG-Node#readme"
}
